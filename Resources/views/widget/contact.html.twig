{{ form_row(form.email) }}
{{ form_row(form.phones) }}

<div class="row">
    <div class="col-md offset-md-8">
        <div class="form-group">
            <button class="btn btn-block btn-primary" type="button" data-trexima-european-cv-dynamic-collection-add="#{{ form.phones.vars.id }}">
                <i class="fa fa-plus" aria-hidden="true"></i>
                Pridať telefón
            </button>
        </div>
    </div>
</div>